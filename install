#!/bin/bash
apt install python ffmpeg && pip install youtube-dl
chmod +x ~/bin/termux-url-opener && echo "Termux share button activated"
chmod +x ~/.shortcuts/update && echo "Termux widget shortcut added"
